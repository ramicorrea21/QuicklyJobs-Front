export default function Request({params: {user_id, service_id}} : {params: {service_id: string, user_id: string}}){
    console.log(user_id, service_id)
    
    return(
        <div>

        </div>
    )
}